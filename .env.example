# Base site URL used for sitemap and robots
BASE_URL=https://flashnews360.gt.tc

# Optional path prefix for post URLs in the sitemap
POST_PATH=/pages/post.php?id=

# Database connection for post.php (Supabase/PostgreSQL)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=flashnews360
DB_USER=flashnews_user
DB_PASS=change_me
DB_SSL=require
